name: Publish Markdig.FetchExtension.SqlServer

on:
  push:
    tags:
      - 'fetchextension-sqlserver-v*.*.*'

permissions:
  id-token: write
  contents: read

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Extract version from tag
      id: get_version
      run: |
        TAG=${GITHUB_REF#refs/tags/fetchextension-sqlserver-v}
        echo "VERSION=$TAG" >> $GITHUB_OUTPUT
        echo "Publishing version: $TAG"

    - name: Restore dependencies
      run: dotnet restore Mostlylucid.Markdig.FetchExtension.SqlServer/Mostlylucid.Markdig.FetchExtension.SqlServer.csproj

    - name: Build base package
      run: dotnet build Mostlylucid.Markdig.FetchExtension/Mostlylucid.Markdig.FetchExtension.csproj --configuration Release --no-restore

    - name: Build
      run: dotnet build Mostlylucid.Markdig.FetchExtension.SqlServer/Mostlylucid.Markdig.FetchExtension.SqlServer.csproj --configuration Release --no-restore -p:Version=${{ steps.get_version.outputs.VERSION }}

    - name: Run tests
      run: dotnet test Mostlylucid.Markdig.FetchExtension.Tests/Mostlylucid.Markdig.FetchExtension.Tests.csproj --configuration Release --verbosity normal

    - name: Pack
      run: dotnet pack Mostlylucid.Markdig.FetchExtension.SqlServer/Mostlylucid.Markdig.FetchExtension.SqlServer.csproj --configuration Release --no-build -p:PackageVersion=${{ steps.get_version.outputs.VERSION }} --output ./artifacts

    - name: Publish to NuGet
      run: dotnet nuget push ./artifacts/*.nupkg --source https://api.nuget.org/v3/index.json --skip-duplicate
